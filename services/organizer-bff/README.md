# Organizer BFF

Organizer/admin-focused backend-for-frontend module.

## Endpoints (handler paths)
- `GET /registrations`
- `POST /registrations/approve`
- `GET /overview` (composed registration + ticket + schedule + room view)
