# Attendee BFF

Attendee-focused backend-for-frontend module.

## Endpoints (handler paths)
- `GET /agenda`
- `GET /tickets`
- `POST /check-in`
- `GET /notifications`
- `GET /overview` (composed registration + ticket + schedule + room view)
